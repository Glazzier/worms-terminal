export default function cls(args) {
  // Limpiar la pantalla de la terminal simulada.
  const output = document.getElementById("output");
  output.innerHTML = "";
  return ""; // Devuelve una cadena vacía, ya que no hay mensaje para mostrar.
}
